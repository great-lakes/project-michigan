import { connect } from 'react-redux'
import HackathonDropdown from './HackathonDropdown'

const mapStateToProps = (state) => ({

})

const mapDispatchToProps = (dispatch) => ({

})

const HackathonDropdownContainer = connect(mapStateToProps, mapDispatchToProps)(HackathonDropdown)

export default HackathonDropdownContainer
